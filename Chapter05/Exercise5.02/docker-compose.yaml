name: exercise5-02
services:

  print:
    image: busybox:1.37.0-glibc

    environment:
      - ENV_FROM_COMPOSE_FILE=HELLO
      - ENV_FROM_SHELL

    env_file:
      - print.env

    command: sh -c 'sleep 5 && env'
