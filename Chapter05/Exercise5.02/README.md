
# The Docker Workshop - Chapter 05 - Exercise 5.02

    docker run -t --rm -v ${PWD}:/app zavoloklom/dclint ./docker-compose.yaml

    docker compose up
    docker compose ps
    docker compose down
