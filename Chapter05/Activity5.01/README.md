# The Docker Workshop - Chapter 05 - Activity 5.01

    docker run -t --rm -v ${PWD}:/app zavoloklom/dclint ./docker-compose.yaml

    docker compose up --detach
    docker compose ps
    docker compose down
