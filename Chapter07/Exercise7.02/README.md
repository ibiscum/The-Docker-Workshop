# The Docker Workshop - Chapter 07 - Exercise 7.02

    docker container run -it --name testsize alpine:3.23.3
    docker image ls
    docker container ls -s
    apk update
    apk upgrade
    apk add --no-cache wget
    docker info
