# The Docker Workshop - Chapter 03 - Exercise 3.04

    docker run --rm -i hadolint/hadolint < Dockerfile

    docker pull scratch
    g++ -o test -static test.c
    docker build -t exercise3.04:1.0 .
    docker run exercise3.04:1.0
    docker images exercise3.04:1.0
    docker history exercise3.04:1.0

